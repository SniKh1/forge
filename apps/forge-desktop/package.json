{
  "name": "forge-desktop-rebuild",
  "private": true,
  "version": "0.4.11",
  "type": "module",
  "scripts": {
    "dev": "npm run sync:forge && vite",
    "dev:tauri": "node ./scripts/run-dev-server.mjs",
    "build": "npm run sync:forge && node ./scripts/run-tsc.mjs -b && vite build",
    "preview": "vite preview",
    "sync:forge": "node ./scripts/sync-forge-sources.mjs",
    "test": "npm run sync:forge && vitest run",
    "tauri:dev": "npm run sync:forge && tauri dev",
    "tauri:build": "npm run sync:forge && tauri build",
    "tauri": "tauri",
    "verify": "npm run sync:forge && node ./scripts/run-tsc.mjs -b && vite build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "lucide-react": "^0.542.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0",
    "@types/react": "^19.1.13",
    "@types/react-dom": "^19.1.9",
    "@vitejs/plugin-react": "^5.0.0",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
