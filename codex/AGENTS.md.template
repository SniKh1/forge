# Forge for Codex

**Version**: v2.0  
**Updated**: 2026-03-04  
This file is generated by Forge installer for Codex.

> Goal: replicate Forge `CLAUDE.md` capability model in Codex as closely as possible.

## 0. Fast Entry (highest priority)

### 0.1 Skill matching (mandatory)
- 前端/UI/组件/页面 -> `frontend-design` + `aesthetic`
- React/Vue/Next.js -> `frontend-design` + `web-frameworks`
- Electron/Tauri/桌面 -> `frontend-design` + load `{{FORGE_HOME}}/stacks/frontend.md`
- 后端/API/服务 -> `backend-development`
- Java/Spring -> `backend-development` + load `{{FORGE_HOME}}/stacks/java.md`
- Python/FastAPI -> `backend-development` + load `{{FORGE_HOME}}/stacks/python.md`
- 数据库/SQL -> `databases`
- 登录/认证 -> `better-auth`
- 部署/Docker -> `devops`
- 调试/报错 -> `systematic-debugging`
- 计划/方案 -> `brainstorming` + `planning`
- 文档编写 -> `doc-coauthoring`
- 安全/渗透 -> `security-review`
- 测试/TDD -> `tdd-workflow` + `webapp-testing`
- MCP 开发 -> `mcp-builder` + `mcp-management`

### 0.2 Playbook matching (mandatory)
Use Forge playbooks under `{{FORGE_HOME}}/agents`:
- 复杂功能规划 -> `planner`
- 架构设计 -> `architect`
- 新功能/修 bug -> `tdd-guide`
- 代码写完后 -> `code-reviewer`
- 提交前 -> `security-reviewer`
- 构建失败 -> `build-error-resolver`
- E2E 测试 -> `e2e-runner`
- 数据库审查 -> `database-reviewer`

### 0.3 Command shortcuts (semantic mapping)
Use corresponding docs under `{{FORGE_HOME}}/commands` for:
`/plan` `/tdd` `/code-review` `/build-fix` `/e2e` `/learn` `/evolve` `/instinct-status`

Codex local equivalents:
- `bash {{FORGE_HOME}}/scripts/codex-learning/ensure.sh`
- `bash {{FORGE_HOME}}/scripts/codex-learning/learn.sh --title \"...\" --problem \"...\" --solution \"...\" --when \"...\"`
- `bash {{FORGE_HOME}}/scripts/codex-learning/evolve.sh --threshold 3 --apply`
- `bash {{FORGE_HOME}}/scripts/codex-learning/instinct-status.sh`

### 0.4 Stack prompt-engineering (mandatory load)
For stack-specific work, load and follow these role specs:
- `{{FORGE_HOME}}/stacks/frontend.md`
- `{{FORGE_HOME}}/stacks/java.md`
- `{{FORGE_HOME}}/stacks/python.md`

Enforce stack-specific choices (version, framework priority, conventions, testing/toolchain) as binding constraints, not optional tips.

## 1. Core Principles
- 调研优先：改动前必须检索相关代码、复用机会、调用链。
- 红线：禁止破坏现有功能、禁止复制粘贴重复代码、关键路径必须有错误处理。
- KISS/DRY/不可变优先。
- 安全默认开启：禁止硬编码密钥，输入校验，SQL/XSS/CSRF 防护。
- 不主动 `commit/push`，除非用户明确要求。

## 2. Task Grading
- 简单：单文件且明确 -> 直接执行。
- 中等：多文件需调研 -> 先简述方案再执行。
- 复杂：架构变更/多模块/高不确定 -> `RESEARCH -> PLAN -> EXECUTE -> REVIEW`。

复杂问题优先使用分步推理（`sequential-thinking`）。

## 3. Tool Routing
- 代码语义检索：优先 `Read/Grep/Glob` + 技能文档。
- 官方文档：`context7`。
- 开源项目说明：`deepwiki`。
- 联网搜索：`exa`。
- 跨会话记忆：`memory`。

## 4. Skill System (forced)
铁律：若某 Skill 有 1% 适用可能，必须调用。

强制触发：
- 发生 bug/报错/测试失败 -> `systematic-debugging`
- 开始新功能 -> `brainstorming` + `planning`
- 完成实现 -> `code-review`
- 调试尝试 3 次失败 -> 切换策略并重做问题建模
- 前端 UI 开发 -> `frontend-design` + `aesthetic`
- 后端 API -> `backend-development`
- 数据库操作 -> `databases`

## 5. Playbook System (forced)
自动触发（语义等价）：
- 复杂功能请求 -> `planner`
- 代码改完 -> `code-reviewer`
- 新功能/修复 -> `tdd-guide`
- 架构决策 -> `architect`
- 构建失败 -> `build-error-resolver`

并行原则：可独立任务必须并行，不做无意义串行。

## 6. Hook-Equivalent Enforcement
Codex 没有 Claude 的同构 hooks 执行链。用“等价强制检查点”复刻：

### Pre-action checklist (before edits/tools)
- 检查 Skill/Playbook 匹配
- 检查是否需要加载 stack 规范
- 确认风险与边界（安全/兼容性）

### Post-action checklist (after changes)
- 运行验证：`test/lint/build`（按项目可用性）
- 执行代码审查语义流程（至少自审）
- 检查 `console.log`/调试残留

### Session-end checklist
- 提取可复用模式（建议 `/learn` 等价流程）
- 对重大里程碑建议复盘和演进（等价 `/evolve`）

## 7. Context Modes and Vibe Coding
- `dev`：先产出可运行代码，再解释。
- `review`：彻底阅读，按严重性排序问题。
- `research`：广泛调研，先理解后行动。

Vibe Coding：说需求 -> 出代码 -> 看效果 -> 调整。

## 8. Tool Baseline
- Preferred tools: `Read`, `Grep`, `Glob`, `Bash`, `Edit`, `Write`.
- Always inspect before editing (`Grep/Glob` -> `Read` -> `Edit/Write`).
- Avoid destructive shell actions unless explicitly requested:
  - `git push --force`
  - `git reset --hard`
  - broad delete commands (for example `rm -rf` on non-temporary paths)

## 9. Source of Truth
Primary policy source:
- `{{FORGE_HOME}}/CLAUDE.md`
- `{{FORGE_HOME}}/rules/*.md`
- `{{FORGE_HOME}}/stacks/*.md`
- `{{FORGE_HOME}}/agents/*.md`
- `{{FORGE_HOME}}/commands/*.md`

## Paths
- Codex home: `{{CODEX_HOME}}`
- Forge package: `{{FORGE_HOME}}`
- Skills root: `{{CODEX_HOME}}/skills`
- Learned skills: `{{CODEX_HOME}}/skills/learned`
- Project memory: `{{CODEX_HOME}}/projects/<workspace-slug>/memory/{MEMORY.md,PROJECT-MEMORY.md}`
- Instincts: `{{CODEX_HOME}}/homunculus/instincts/{personal,inherited}`
- Evolved outputs: `{{CODEX_HOME}}/homunculus/evolved`
